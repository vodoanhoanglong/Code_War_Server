CREATE TABLE "public"."discusses" ("id" text NOT NULL DEFAULT gen_random_uuid(), "floor" text, "title" text, "content" text, "status" text NOT NULL DEFAULT '"active"', "createdAt" Timestamp NOT NULL DEFAULT now(), "createdBy" text, "updatedAt" timestamptz NOT NULL DEFAULT now(), "updatedBy" text, "accountId" text NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY ("accountId") REFERENCES "public"."account"("id") ON UPDATE restrict ON DELETE restrict);
