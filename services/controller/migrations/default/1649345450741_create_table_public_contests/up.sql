CREATE TABLE "public"."contests" ("id" text NOT NULL DEFAULT gen_random_uuid(), "name" text NOT NULL, "des" text, "startDate" timestamptz, "endDate" timestamptz, "logoUrl" text, "status" text NOT NULL DEFAULT '"active"', "createdAt" timestamptz NOT NULL DEFAULT now(), "createdBy" text, "updatedAt" timestamptz NOT NULL DEFAULT now(), "updatedBy" text, PRIMARY KEY ("id") , UNIQUE ("name"));
